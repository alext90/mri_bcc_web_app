[project]
name = "dl_web_app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi", "python-multipart", "uvicorn", "httpx", "torch", "torchvision", "matplotlib", "pytorch-lightning", "sqlalchemy", "psycopg2-binary"]
